ServerRoot "/usr/local/apache2"

Listen 80

LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule dir_module modules/mod_dir.so
LoadModule mime_module modules/mod_mime.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule headers_module modules/mod_headers.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule alias_module modules/mod_alias.so
LoadModule filter_module modules/mod_filter.so
LoadModule deflate_module modules/mod_deflate.so

User daemon
Group daemon

ServerAdmin admin@localhost
ServerName localhost

DocumentRoot "/usr/local/apache2/htdocs"
DirectoryIndex index.html

ErrorLog /proc/self/fd/2
LogLevel warn
CustomLog /proc/self/fd/1 common

TypesConfig conf/mime.types
AddType application/javascript js mjs
AddType text/css css

<Directory "/usr/local/apache2/htdocs">
  Options Indexes FollowSymLinks
  AllowOverride All
  Require all granted

  RewriteEngine On

  # Proxy API to NestJS (backend uses host network → localhost:3001)
  RewriteRule ^api/?(.*)$ http://host.docker.internal:3001/api/$1 [P,L,NE]

  # SPA fallback
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</Directory>

ProxyPreserveHost On
ProxyPass /api http://host.docker.internal:3001/api
ProxyPassReverse /api http://host.docker.internal:3001/api

<FilesMatch "\.(html)$">
  Header set Cache-Control "no-cache, no-store, must-revalidate"
  Header set Pragma "no-cache"
  Header set Expires "0"
</FilesMatch>

LimitRequestBody 52428800
